<%- include('../partials/header') %>

<table id="list">
    <thead>
        <tr>
            <th>Recipes</th>
        </tr>
    </thead>
    <tbody>
        <% recipes.forEach(function(m){ %>
            <td><%= m.recipe %></td>
            <td><a href="/recipes/<%= m.id %>">DETAILS</a></td>
        </tr>
        <% }) %>
    </tbody>
</table>
<%- include('../partials/footer') %>